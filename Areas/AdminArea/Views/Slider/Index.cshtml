@model IEnumerable<Slider>

<div class="col-lg-6 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Hoverable Table</h4>
            <p class="card-description">
                <a class="btn btn-primary" asp-controller="Slider" asp-action="Create"> Create Slider</a>
            </p>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Image</th>
                        <th>Settings</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <img src="~/Main-Images/@item.ImageUrl"  style="width:120px; height:120px" />
                            </td>
                            <td>
                                <a asp-controller="Slider" asp-action="Update" asp-route-id="@item.Id" class="btn btn-success">
                                    <i class="icon-credit-card"></i>
                                </a>
                            </td>
                            <td>
                                <a asp-controller="Slider" asp-action="Detail" asp-route-id="@item.Id" class="btn btn-info">
                                    <i class="icon-info"></i>
                                </a>
                            </td>
                            <td>
                                <a asp-controller="Slider" asp-action="Remove" asp-route-id="@item.Id" class="btn btn-danger">
                                    <i class="icon-magnifier-remove"></i>
                                </a>
                            </td>
                        </tr>
                     }

                </tbody>
            </table>
        </div>
    </div>
</div>